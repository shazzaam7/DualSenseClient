namespace DualSenseClient.Core.DualSense.Enums;

public enum LightbarBehavior
{
    /// <summary>
    /// Pulses the lightbar blue and stays on (Default)
    /// </summary>
    Default = 0x1,

    /// <summary>
    /// Allows for custom lightbar color to be set
    /// </summary>
    Custom = 0x2
}